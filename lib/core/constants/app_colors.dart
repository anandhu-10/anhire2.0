import 'package:flutter/material.dart';

class AppColors {
  // Dark theme text colors
  static const textPrimary = Color(0xFFFFFFFF);     // main text
  static const textSecondary = Color(0xFFCAC4D0);   // labels & subtitles
  static const textMuted = Color(0xFF9F99A8);      // hints & secondary labels
  static const textOnPurple = Color(0xFFFFFFFF);    // text on purple backgrounds
  static const textOnLavender = Color(0xFF1C1B1F);  // dark text on light lavender
  static const textAccent = Color(0xFFD0BCFF);      // purple accent text

  // Backgrounds
  static const bgDark = Color(0xFF1C1B1F);
  static const bgSurface = Color(0xFF2B2930);
  static const bgCard = Color(0xFF2B2930);
  static const bgPurple = Color(0xFF6750A4);
  static const bgLavender = Color(0xFFEADDFF);

  // Chips
  static const chipInactiveBg = Color(0xFF2B2930);
  static const chipInactiveText = Color(0xFFFFFFFF);
  static const chipActiveBg = Color(0xFF6750A4);
  static const chipActiveText = Color(0xFFFFFFFF);
  static const chipBorder = Color(0xFF3A383F);
}
